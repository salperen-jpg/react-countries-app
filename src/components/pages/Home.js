import React from 'react';
import Countries from '../Countries';
import Search from '../Search';

const Home = () => {
  return (
    <main>
      <Search />
      <Countries />
    </main>
  );
};

export default Home;
